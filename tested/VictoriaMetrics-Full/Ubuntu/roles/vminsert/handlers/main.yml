---
- name: restart vminsert
  ansible.builtin.systemd:
    name: vminsert
    state: restarted
    daemon_reload: true
  become: true

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true